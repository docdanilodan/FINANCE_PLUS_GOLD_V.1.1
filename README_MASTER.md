# APP_NUOVA_SETT_IA_00 MASTER

Build consolidata **2.0.0-stage2-rc2** successiva all'approvazione della matrice FASE 1-5 e al secondo stadio di collaudo.

Questa cartella contiene l'architettura MASTER modulare destinata al repository `docdanilodan/FINANCE_PLUS_GOLD_V.1.1`, branch di lavoro `app-nuova-sett-ia-00-master`.

La build conserva provenienza e compatibilita storiche. Lo schema MASTER e stato applicato al database Neon di produzione il 16/09/2026 dopo preflight; le tabelle confliggenti precedenti sono state preservate con prefisso `pre_master_*_20260916` e prima del cutover e stato creato uno snapshot di rollback.

Per lo stato verificato consultare `docs/TEST_REPORT.md` e `provenance/stage2_status.csv`.
